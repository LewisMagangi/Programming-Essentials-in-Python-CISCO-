#Recursive implementation of the factorial function.
global n = 0
def factorial(n):
    if n == 1:    # The base case (termination condition.)
        return 1
    else:
        return n * factorial(n - 1)

n = int(input("Enter the no to be factorised :" ))
print(factorial(n)) # 4 * 3 * 2 * 1 = 24
